# Version note

This v2 package updates the optional base-R cross-check so that it reads `data/analysis_minimal_full_reproducibility.csv` directly. The Python workflow and expected numerical results are unchanged.
